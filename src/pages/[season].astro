---
import { loadAllSeasons } from '../lib/data';
import SeasonView from '../components/SeasonView.astro';

export async function getStaticPaths() {
  return loadAllSeasons().map(s => ({ params: { season: s.id } }));
}

const { season } = Astro.params;
---

<SeasonView seasonId={season!} />
